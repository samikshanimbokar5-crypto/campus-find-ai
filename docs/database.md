# Database

Collections are User, Item, Match, and Notification. Item fields are indexed for type, category, color, location, date, status, owner, and text search. Match has a unique lost/found pair index. Password hashes are excluded by default from User queries.
