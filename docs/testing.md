# Testing

Backend tests run with Node's built-in test runner. Current tests cover the health response and authentication boundary. Add MongoDB-backed integration tests for registration, login, item creation, matching, ownership, and invalid input before production release.
